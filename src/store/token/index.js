import tokenState from "./tokenState"
import tokeGetters from "./tokenGetters"
import tokenMutations from "./tokenMutations"

export default {
  state: tokenState,
  getters: tokeGetters,
  mutations: tokenMutations,
}
